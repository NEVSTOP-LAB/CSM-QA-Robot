# AI-013 配置校验修复

- **问题**：`tests/test_config.py::test_article_type_valid` 仅允许 `article`/`question`，与 `config/articles.yaml` 及主流程支持的 `column`、`user_answers` 类型不一致，导致基准测试失败。
- **修复**：将允许的类型扩展为 `{article, question, column, user_answers}`，与 `docs/plan/README.md` 中的配置约定保持一致。
- **验证**：`python -m pytest tests/ -v` 通过。
